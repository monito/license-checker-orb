# Monorepo-correct production license reporting — Implementation Plan (v5.0.0)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the `monito/license-checker` orb produce a correct, complete production license report for any repository (single-package or workspaces monorepo) across npm / yarn / yarn-berry / pnpm, by pruning `node_modules` to the production closure instead of trusting `license-checker --production`.

**Architecture:** The `validate` command produces two reports — `<name>-all.csv` (full install, the universal forbidden-license gate) and `<name>-production.csv` (built after pruning `node_modules` with the package manager's own production-install, then a plain scan). The prune command is selected by a `case` on a new `pkg-manager` parameter. The previous `-development.csv` report is replaced by `-all.csv`. This is a breaking output change → major version bump to **5.0.0**.

**Tech Stack:** CircleCI orb YAML (Orb Development Kit layout under `src/`), `license-checker` npm CLI, package managers npm/yarn/yarn-berry/pnpm, `circleci` CLI for local packing.

**Reference spec:** [docs/superpowers/specs/2026-06-03-monorepo-production-report-design.md](../specs/2026-06-03-monorepo-production-report-design.md)

---

## File structure

| File | Responsibility | Action |
|---|---|---|
| `data-fixtures-monorepo/package.json` | Root of yarn-berry workspaces test fixture (private, no deps, workspaces, root devDep) | Create |
| `data-fixtures-monorepo/.yarnrc.yml` | Force `nodeLinker: node-modules` so license-checker can scan | Create |
| `data-fixtures-monorepo/apps/app-a/package.json` | Workspace app with a real MIT production dep (`ms`) | Create |
| `data-fixtures-monorepo/yarn.lock` | Committed lockfile (immutable install in CI) | Create (generated) |
| `data-fixtures-monorepo/README.md` | Explains what the fixture is for | Create |
| `src/commands/validate.yml` | Core: uniform prune + two-report flow | Rewrite |
| `src/commands/validate-copyleft.yml` | Pass `pkg-manager` through to `validate` | Modify |
| `src/jobs/validate.yml` | Add `pnpm` enum, pass `pkg-manager` to command | Modify |
| `src/jobs/validate-copyleft.yml` | Add `pnpm` enum, pass `pkg-manager` to command | Modify |
| `.circleci/test-deploy.yml` | Add yarn-berry monorepo integration job + assertions + `requires` | Modify |
| `README.md` | Document monorepo support, nodeLinker requirement, report rename | Modify |
| `CHANGELOG.md` | `5.0.0` entry | Modify |

**Local validation command used throughout:** `circleci orb pack src > /tmp/packed.yml` — must succeed (exit 0). Full schema validation (`circleci orb validate`) requires CircleCI API auth and runs in CI via `orb-tools`; it cannot run locally here, so packing is the local gate.

---

## Task 1: Create the yarn-berry monorepo test fixture (and prove the fix mechanism)

This fixture is also the local "test": before the orb change, `license-checker --production` returns only the root package; after the prune, the workspace app's production dep appears. All values below are verified working with Yarn 4.9.1 + `license-checker`.

**Files:**
- Create: `data-fixtures-monorepo/package.json`
- Create: `data-fixtures-monorepo/.yarnrc.yml`
- Create: `data-fixtures-monorepo/apps/app-a/package.json`
- Create: `data-fixtures-monorepo/README.md`
- Create: `data-fixtures-monorepo/yarn.lock` (generated by yarn)

- [ ] **Step 1: Write the fixture files**

`data-fixtures-monorepo/package.json`:

```json
{
  "name": "@monito/test-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "yarn@4.9.1",
  "workspaces": ["apps/*"],
  "devDependencies": {
    "chalk": "^5.4.1"
  }
}
```

`data-fixtures-monorepo/.yarnrc.yml`:

```yaml
nodeLinker: node-modules
enableTelemetry: false
```

`data-fixtures-monorepo/apps/app-a/package.json`:

```json
{
  "name": "@monito/test-app-a",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "ms": "2.1.3"
  }
}
```

`data-fixtures-monorepo/README.md`:

```markdown
# Monorepo test fixture (yarn-berry workspaces)

Used by the orb's integration tests to verify monorepo-correct production
reporting. The root is `private` with **no `dependencies`** and only a
`devDependencies` entry (`chalk`); the real production dependency (`ms`, MIT)
lives in the `apps/app-a` workspace, mirroring a typical workspaces monorepo.

`.yarnrc.yml` sets `nodeLinker: node-modules` because `license-checker` scans
`node_modules` (Yarn Berry's default PnP linker has none).

Expectations after the orb runs:
- `*-production.csv` contains `ms` (the workspace app's prod dep) — not just the root.
- `*-all.csv` contains both `ms` and `chalk`.
- A forbidden-license gate over the production closure that forbids `MIT` fails.
```

- [ ] **Step 2: Generate the committed lockfile**

Run (network required; uses the repo-local corepack yarn):

```bash
cd data-fixtures-monorepo && corepack yarn@4.9.1 install && cd ..
```

Expected: completes with `Done in ...`, creates `data-fixtures-monorepo/yarn.lock`. Confirm `node_modules` was created (it will be git-ignored by the repo root `.gitignore`).

- [ ] **Step 3: Verify BEFORE-state (demonstrates the bug)**

Run:

```bash
cd data-fixtures-monorepo && npx --yes license-checker --production --csv | sed 's/,.*//' ; cd ..
```

Expected: output lists only the header and `"@monito/test-monorepo@1.0.0"` — **no `ms`**. This is the broken behavior the change fixes.

- [ ] **Step 4: Verify AFTER-prune state (the fix)**

Run:

```bash
cd data-fixtures-monorepo && \
  corepack yarn@4.9.1 workspaces focus --all --production && \
  npx --yes license-checker --csv | grep -iE '"(ms|chalk)@' ; \
  echo "exit:$?" ; cd ..
```

Expected: output contains `"ms@2.1.3"` and does **not** contain `chalk` (root devDep pruned). This proves the production closure now includes the workspace app's prod dep.

- [ ] **Step 5: Restore the full install (so later tasks start clean) and verify negative gate**

Run:

```bash
cd data-fixtures-monorepo && corepack yarn@4.9.1 install && \
  corepack yarn@4.9.1 workspaces focus --all --production && \
  ( npx --yes license-checker --csv --failOn MIT >/dev/null 2>&1 && echo "GATE-DID-NOT-TRIP" || echo "GATE-TRIPPED" ) ; \
  corepack yarn@4.9.1 install ; cd ..
```

Expected: prints `GATE-TRIPPED` (a forbidden `MIT` in the workspace app's production deps fails the check). The final `yarn install` restores the full install.

- [ ] **Step 6: Commit**

```bash
git add data-fixtures-monorepo
git commit -m "test: add yarn-berry workspaces monorepo fixture"
```

---

## Task 2: Rewrite the `validate` command (uniform prune + two reports)

**Files:**
- Modify (full rewrite): `src/commands/validate.yml`

- [ ] **Step 1: Replace the file with the new content**

```yaml
description: >
  Command to validate the licenses used by your project.
  Two CSV reports are produced as build artifacts:
    <report-name>-production.csv  the production dependency closure only.
    <report-name>-all.csv         every installed dependency (production + development).
  The production report is built by pruning node_modules to the production closure with the
  project's package manager, then scanning what remains. This is correct for single-package
  repos and workspaces monorepos alike (it does not rely on license-checker's --production flag,
  which only understands the root package.json).
  A semicolon separated list of forbidden licenses can be provided; if one is found the build fails.

parameters:
  app-dir:
    type: string
    default: .
    description: The location of the package.json and associated node_modules
  pkg-manager:
    type: enum
    enum:
      - npm
      - yarn
      - yarn-berry
      - pnpm
    default: yarn
    description: >
      The package manager used to install the node modules. It selects how node_modules is
      pruned to the production closure before the production report is generated
      (npm: "npm ci --omit=dev"; yarn: "yarn install --production --frozen-lockfile";
      yarn-berry: "yarn workspaces focus --all --production"; pnpm: "pnpm install --prod --frozen-lockfile").
      Job callers inherit this from the job's pkg-manager; direct command callers must set it
      to match their project.
  exclude-private:
    type: boolean
    default: true
    description: >
      When true (the default), private packages are excluded from the checks and the reports.
  forbidden:
    type: string
    default: ""
    description: >
      A semi colon separated list of forbidden licenses.
      If no value is provided the license check won't fail.
  production-only:
    type: boolean
    default: false
    description: >
      When true, only the production dependencies are checked and reported (no -all report).
      By default both the production and the all-dependencies reports are produced, and the
      forbidden-license gate runs against the all-dependencies set (a superset of production).
  report-name:
    type: string
    default: license-checker-report
    description: >
      Base name of the license checker report artifacts.
      Reports are suffixed with "-production" and "-all".
steps:
  - run:
      name: Configure license-checker options
      command: |
        if [ "<< parameters.exclude-private >>" = "true" ]; then
          echo 'export LC_EXCLUDE_PRIVATE="--excludePrivatePackages"' >> "$BASH_ENV"
        else
          echo 'export LC_EXCLUDE_PRIVATE=""' >> "$BASH_ENV"
        fi
  - when:
      condition:
        not: << parameters.production-only >>
      steps:
        - run:
            name: Build all-dependencies license report
            command: npx license-checker $LC_EXCLUDE_PRIVATE --csv > /tmp/<< parameters.report-name >>-all.csv
            working_directory: << parameters.app-dir >>
            when: always
        - run:
            name: All-dependencies license check
            command: npx license-checker $LC_EXCLUDE_PRIVATE --summary --failOn "<< parameters.forbidden >>"
            working_directory: << parameters.app-dir >>
        - run:
            name: All-dependencies license summary
            command: npx license-checker $LC_EXCLUDE_PRIVATE --summary
            working_directory: << parameters.app-dir >>
            when: on_fail
  - run:
      name: Prune node_modules to the production closure
      working_directory: << parameters.app-dir >>
      when: always
      command: |
        case "<< parameters.pkg-manager >>" in
          npm)
            npm ci --omit=dev
            ;;
          yarn)
            yarn install --production --frozen-lockfile
            ;;
          yarn-berry)
            yarn workspaces focus --all --production
            ;;
          pnpm)
            pnpm install --prod --frozen-lockfile
            ;;
          *)
            echo "Unsupported pkg-manager: << parameters.pkg-manager >>" >&2
            exit 1
            ;;
        esac
  - run:
      name: Build production license report
      command: npx license-checker $LC_EXCLUDE_PRIVATE --csv > /tmp/<< parameters.report-name >>-production.csv
      working_directory: << parameters.app-dir >>
      when: always
  - when:
      condition: << parameters.production-only >>
      steps:
        - run:
            name: Production license check
            command: npx license-checker $LC_EXCLUDE_PRIVATE --summary --failOn "<< parameters.forbidden >>"
            working_directory: << parameters.app-dir >>
        - run:
            name: Production license summary
            command: npx license-checker $LC_EXCLUDE_PRIVATE --summary
            working_directory: << parameters.app-dir >>
            when: on_fail
  - store_artifacts:
      path: /tmp/<< parameters.report-name >>-production.csv
      destination: << parameters.app-dir >>/<< parameters.report-name >>-production.csv
      when: always
  - when:
      condition:
        not: << parameters.production-only >>
      steps:
        - store_artifacts:
            path: /tmp/<< parameters.report-name >>-all.csv
            destination: << parameters.app-dir >>/<< parameters.report-name >>-all.csv
            when: always
```

- [ ] **Step 2: Pack to validate the orb structure**

Run: `circleci orb pack src > /tmp/packed.yml && echo PACK_OK`
Expected: prints `PACK_OK` (exit 0, no YAML/structure error).

- [ ] **Step 3: Commit**

```bash
git add src/commands/validate.yml
git commit -m "feat!: build production report by pruning to the production closure

Replaces license-checker --production (root-only, broken for workspaces) with a
package-manager prune + plain scan. Adds the pkg-manager parameter and renames the
second report from -development.csv to -all.csv."
```

---

## Task 3: Pass `pkg-manager` through the `validate-copyleft` command

**Files:**
- Modify: `src/commands/validate-copyleft.yml`

- [ ] **Step 1: Add the `pkg-manager` parameter**

In `src/commands/validate-copyleft.yml`, add this parameter to the `parameters:` block (place it right after the `app-dir` parameter, before `exclude-private`):

```yaml
  pkg-manager:
    type: enum
    enum:
      - npm
      - yarn
      - yarn-berry
      - pnpm
    default: yarn
    description: >
      The package manager used to install the node modules. Passed through to the validate
      command, where it selects how node_modules is pruned to the production closure.
```

- [ ] **Step 2: Pass it through in the `steps:` block**

Change the `validate` step invocation at the bottom of the file from:

```yaml
steps:
  - validate:
      app-dir: << parameters.app-dir >>
      report-name: << parameters.report-name >>
      forbidden: << parameters.forbidden >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
```

to:

```yaml
steps:
  - validate:
      app-dir: << parameters.app-dir >>
      pkg-manager: << parameters.pkg-manager >>
      report-name: << parameters.report-name >>
      forbidden: << parameters.forbidden >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
```

- [ ] **Step 3: Update the command description**

Change the description line `A CSV report is uploaded as a build artifact for each dependency set (production and development).` to:

```
A CSV report is uploaded as a build artifact for each dependency set (production and all dependencies).
```

- [ ] **Step 4: Pack and commit**

```bash
circleci orb pack src > /tmp/packed.yml && echo PACK_OK
git add src/commands/validate-copyleft.yml
git commit -m "feat: thread pkg-manager through validate-copyleft command"
```

Expected: `PACK_OK`.

---

## Task 4: Add `pnpm` to the jobs and pass `pkg-manager` to the commands

**Files:**
- Modify: `src/jobs/validate.yml`
- Modify: `src/jobs/validate-copyleft.yml`

- [ ] **Step 1: `src/jobs/validate.yml` — add `pnpm` to the enum**

Change the `pkg-manager` parameter's enum from:

```yaml
  pkg-manager:
    type: enum
    enum:
      - npm
      - yarn
      - yarn-berry
    default: yarn
    description: The package manager to use to install the node modules
```

to:

```yaml
  pkg-manager:
    type: enum
    enum:
      - npm
      - yarn
      - yarn-berry
      - pnpm
    default: yarn
    description: The package manager to use to install the node modules
```

- [ ] **Step 2: `src/jobs/validate.yml` — pass `pkg-manager` to the command**

Change the `validate` step invocation from:

```yaml
  - validate:
      forbidden: << parameters.forbidden >>
      app-dir: << parameters.app-dir >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
      report-name: << parameters.report-name >>
```

to:

```yaml
  - validate:
      forbidden: << parameters.forbidden >>
      app-dir: << parameters.app-dir >>
      pkg-manager: << parameters.pkg-manager >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
      report-name: << parameters.report-name >>
```

- [ ] **Step 3: `src/jobs/validate-copyleft.yml` — add `pnpm` to the enum**

Apply the identical enum change as Step 1 (add `      - pnpm` after `      - yarn-berry`).

- [ ] **Step 4: `src/jobs/validate-copyleft.yml` — pass `pkg-manager` to the command**

Change the `validate-copyleft` step invocation from:

```yaml
  - validate-copyleft:
      app-dir: << parameters.app-dir >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
      report-name: << parameters.report-name >>
```

to:

```yaml
  - validate-copyleft:
      app-dir: << parameters.app-dir >>
      pkg-manager: << parameters.pkg-manager >>
      production-only: << parameters.production-only >>
      exclude-private: << parameters.exclude-private >>
      report-name: << parameters.report-name >>
```

- [ ] **Step 5: Update both job descriptions**

In both `src/jobs/validate.yml` and `src/jobs/validate-copyleft.yml`, change the description line:

```
A CSV report is uploaded as a build artifact for each dependency set (production and development).
```

to:

```
A CSV report is uploaded as a build artifact for each dependency set (production and all dependencies).
```

- [ ] **Step 6: Pack and commit**

```bash
circleci orb pack src > /tmp/packed.yml && echo PACK_OK
git add src/jobs/validate.yml src/jobs/validate-copyleft.yml
git commit -m "feat: add pnpm support and pass pkg-manager to the commands"
```

Expected: `PACK_OK`.

---

## Task 5: Add the monorepo integration test

**Files:**
- Modify: `.circleci/test-deploy.yml`

- [ ] **Step 1: Add a monorepo integration job**

In `.circleci/test-deploy.yml`, add this job to the `jobs:` map (after the existing `integration-test-commands` job):

```yaml
  integration-test-monorepo:
    docker:
      - image: cimg/node:22.13
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn-berry
          app-dir: data-fixtures-monorepo
      - license-checker/validate:
          app-dir: data-fixtures-monorepo
          pkg-manager: yarn-berry
          exclude-private: false
          report-name: monorepo-report
      - run:
          name: Assert workspace production dep is in the production report
          working_directory: data-fixtures-monorepo
          command: |
            echo "Production report:"; cat /tmp/monorepo-report-production.csv
            grep -iq '"ms@' /tmp/monorepo-report-production.csv \
              || { echo "FAIL: ms (workspace app prod dep) missing from production report"; exit 1; }
            grep -iq '"chalk@' /tmp/monorepo-report-production.csv \
              && { echo "FAIL: chalk (devDependency) leaked into production report"; exit 1; }
            echo "OK: production report contains ms and not chalk"
      - run:
          name: Assert all-dependencies report contains both prod and dev deps
          working_directory: data-fixtures-monorepo
          command: |
            grep -iq '"ms@' /tmp/monorepo-report-all.csv \
              || { echo "FAIL: ms missing from all report"; exit 1; }
            grep -iq '"chalk@' /tmp/monorepo-report-all.csv \
              || { echo "FAIL: chalk missing from all report"; exit 1; }
            echo "OK: all report contains ms and chalk"
      - run:
          name: Assert forbidden license in a workspace prod dep trips the gate
          working_directory: data-fixtures-monorepo
          command: |
            # node_modules is already pruned to the production closure by the validate command above.
            if npx license-checker --csv --failOn MIT >/dev/null 2>&1; then
              echo "FAIL: forbidding MIT did not trip the gate over the production closure"; exit 1
            fi
            echo "OK: forbidding MIT trips the gate (workspace prod dep is seen)"
```

- [ ] **Step 2: Run the monorepo job in the workflow and in `integration-test-commands` requires**

In the `workflows.test-deploy.jobs` list, add the new job with the standard filter (place it alongside the other integration jobs):

```yaml
      - integration-test-monorepo:
          filters: *filters
```

- [ ] **Step 3: Add the job to the publish `requires` list**

In the `orb-tools/publish` job's `requires:` list, add the new job so publishing waits on it:

```yaml
            - integration-test-monorepo
```

(Insert it as a new list item among the existing `requires` entries, e.g. right after `- integration-test-commands`.)

- [ ] **Step 4: Validate the config YAML structure**

Run: `circleci config pack .circleci/test-deploy.yml > /tmp/packed-config.yml && echo CONFIG_PACK_OK`
Expected: prints `CONFIG_PACK_OK` (exit 0). (Full `circleci config validate` requires API auth and runs in CI.)

- [ ] **Step 5: Commit**

```bash
git add .circleci/test-deploy.yml
git commit -m "test: add yarn-berry monorepo integration test"
```

---

## Task 6: Documentation and CHANGELOG (5.0.0)

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: README — update the intro line**

Change line 3 from:

```
A Circle CI orb to check the licenses used by your project (production and development dependencies), based on [license-checker](https://www.npmjs.com/package/license-checker)
```

to:

```
A Circle CI orb to check the licenses used by your project — for both the production dependency closure and all installed dependencies, including in workspaces monorepos — based on [license-checker](https://www.npmjs.com/package/license-checker)
```

- [ ] **Step 2: README — add a "Monorepo & package managers" section**

Insert this section immediately after the `## Usage` section (after line 7's paragraph, before `## Releasing`):

```markdown
## Monorepo & package manager support

The orb supports `npm`, `yarn` (classic v1), `yarn-berry`, and `pnpm` via the
`pkg-manager` parameter, and produces correct production reports for both
single-package repos and **workspaces monorepos**.

Rather than trusting `license-checker --production` (which only understands the
root `package.json` and reports an effectively empty production set in a workspaces
monorepo), the orb prunes `node_modules` to the production closure using the
package manager itself, then scans what remains:

| `pkg-manager` | production prune |
|---|---|
| `npm` | `npm ci --omit=dev` |
| `yarn` | `yarn install --production --frozen-lockfile` |
| `yarn-berry` | `yarn workspaces focus --all --production` |
| `pnpm` | `pnpm install --prod --frozen-lockfile` |

Two CSV artifacts are produced:

- `<report-name>-production.csv` — the production dependency closure only.
- `<report-name>-all.csv` — every installed dependency (production + development).

By default the forbidden-license gate runs against the all-dependencies set
(a superset of production). Set `production-only: true` to gate and report on the
production closure alone.

> **Yarn Berry note:** `license-checker` scans `node_modules`, which Yarn Berry's
> default Plug'n'Play linker does not create. Yarn-berry projects must set
> `nodeLinker: node-modules` in `.yarnrc.yml`.
```

- [ ] **Step 3: CHANGELOG — add the 5.0.0 entry**

Insert this block directly below the `...adheres to [Semantic Versioning]...` line and above `## [4.0.0]`:

```markdown
## [5.0.0]

### Changed

- **BREAKING:** The production report is now built by pruning `node_modules` to the
  production closure with the project's package manager and then scanning it, instead
  of using `license-checker --production`. This fixes effectively-empty production
  reports in Yarn/npm/pnpm **workspaces monorepos**, where production dependencies
  declared in workspace packages were previously misclassified as development.
- **BREAKING:** The second CSV artifact is renamed from `<report-name>-development.csv`
  to `<report-name>-all.csv` and now contains every installed dependency (production +
  development). Update any tooling that consumes the report by its previous path.
- The forbidden-license gate (when not `production-only`) now runs against the
  all-dependencies set.

### Added

- `pnpm` is now a supported `pkg-manager` value (in addition to `npm`, `yarn`, `yarn-berry`).
- `pkg-manager` parameter on the `validate` and `validate-copyleft` commands, selecting
  how `node_modules` is pruned to the production closure.

### Notes

- Yarn Berry projects must use `nodeLinker: node-modules` in `.yarnrc.yml` (required for
  `license-checker` to find `node_modules`).
- Classic `yarn` v1 uses `yarn install --production`; this is accurate for single-package
  repos and best-effort for v1 workspaces (the all-dependencies gate remains the safety net).
```

- [ ] **Step 4: Commit**

```bash
git add README.md CHANGELOG.md
git commit -m "docs: document monorepo support and the 5.0.0 changes"
```

---

## Task 7: Final verification

**Files:** none (verification only)

- [ ] **Step 1: Final pack of the orb source**

Run: `circleci orb pack src > /tmp/packed.yml && echo PACK_OK && wc -l /tmp/packed.yml`
Expected: `PACK_OK` and a line count > 0.

- [ ] **Step 2: Confirm the packed orb contains the new wiring**

Run:

```bash
grep -c 'workspaces focus --all --production' /tmp/packed.yml
grep -c 'report-name >>-all.csv' /tmp/packed.yml
grep -c 'pnpm' /tmp/packed.yml
```

Expected: each prints a count ≥ 1 (prune command present, `-all.csv` report present, pnpm enum present).

- [ ] **Step 3: Re-run the fixture's end-to-end check (independent of CI)**

Run:

```bash
cd data-fixtures-monorepo && corepack yarn@4.9.1 install >/dev/null 2>&1 && \
  corepack yarn@4.9.1 workspaces focus --all --production >/dev/null 2>&1 && \
  npx --yes license-checker --csv | grep -iq '"ms@' && echo "PROD-REPORT-OK" || echo "PROD-REPORT-FAIL" ; \
  corepack yarn@4.9.1 install >/dev/null 2>&1 ; cd ..
```

Expected: prints `PROD-REPORT-OK`.

- [ ] **Step 4: Confirm clean git status**

Run: `git status --short`
Expected: empty (all changes committed). `data-fixtures-monorepo/node_modules` must not appear (covered by the repo `.gitignore` `node_modules` rule).

---

## Self-review notes (spec coverage)

- Production report correctness for monorepos → Task 2 (prune + scan), proven in Task 1 & 7.
- Uniform across managers (npm/yarn/yarn-berry/pnpm) → Task 2 `case`, Task 4 enums.
- `-development.csv` → `-all.csv` rename → Task 2 (report build + store), Task 6 (docs/CHANGELOG).
- yarn v1 uniform prune → Task 2 `yarn) yarn install --production` branch.
- nodeLinker requirement → Task 1 fixture `.yarnrc.yml`, Task 6 README/CHANGELOG.
- Monorepo fixture + assertions (positive + all-report + negative gate) → Task 1, Task 5.
- Backward-compat command default `pkg-manager: yarn` → Task 2 default.
- Major version bump to 5.0.0 → Task 6 CHANGELOG.
- Publish `requires` updated → Task 5 Step 3.
```
